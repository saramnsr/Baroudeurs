# Default values - safe to commit, no real secrets here.
# Override any of these locally by creating a .env.local file
# (which is gitignored and never committed).

DATABASE_URL="mysql://root:@127.0.0.1:3306/baroudeurs?serverVersion=5.7"

# Placeholder - get the real value from a teammate privately,
# then put it in your own .env.local (never here).
CLOUDINARY_URL=cloudinary://YOUR_API_KEY:YOUR_API_SECRET@dy13axswo

APP_ENV=dev
APP_SECRET=change_this_to_a_random_string
