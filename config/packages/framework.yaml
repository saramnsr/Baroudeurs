framework:
  secret: '%env(APP_SECRET)%'
  default_locale: en

  translator:
    default_path: '%kernel.project_dir%/translations'
    fallbacks:
      - en

  session:
    handler_id: null
    cookie_secure: auto
    cookie_samesite: lax

  php_errors:
    log: true